package feed.controllers

import feed.models.User

class UserController() {
    fun getUser(userId: String): User {
    }

    fun addUser(emailId: String): String {
    }

    fun addFriend(userId1: String, userId: String): String {
    }

    fun getFeed(userId: String) {
    }

    fun post(userId: String, text: String): String {
    }
}
